<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>OTP Generator</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #ffffff; /* Set background color to white */
            color: #2d2d2d; /* Set text color to dark navy blue */
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column; /* Align items in a column */
            height: 100vh;
        }

        h1 {
            text-align: center;
            color: #00bcd4;
            margin-bottom: 5px; /* Add margin for spacing */
        }

        h3 {
            text-align: center;
            color: #001f3f; /* Dark navy blue color */
            margin-top: 0; /* Remove default margin for h3 */
        }

        form {
            max-width: 400px;
            margin: 20px auto; /* Add margin for spacing */
            background-color: #363636;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.3);
        }

        label {
            display: block;
            margin-bottom: 10px;
            color: #00bcd4;
            text-align: center;
        }

        input {
            width: 100%;
            padding: 10px;
            margin-bottom: 15px;
            border: 1px solid #00bcd4;
            border-radius: 4px;
            box-sizing: border-box;
            color: #2d2d2d;
            background-color: #ffffff;
        }

        button {
            background-color: #00bcd4;
            color: #ffffff;
            padding: 10px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }

        button:hover {
            background-color: #009688;
        }
    </style>
</head>
<body>

    <h1>USER AUTHORIZATION</h1>
    <h3>For Accessing Video Footages</h3>
    
    <form action="/sendOTP" method="post" style="text-align: center;">
        <label for="email">Enter your email:</label>
        <input type="email" id="email" name="email" required>
        <button type="submit">Generate OTP</button>
    </form>
    <% if (invalidEmail) { %>
        <div style="text-align: center; margin-top: 20px; color: red;">
            You are not authorized to access this database. Kindly contact your administrator!
        </div>
    <% } %>

</body>
</html>
